# 本项目强制执行规范

> 这些不是建议，是**每次动手前必须实际执行的命令**。
> 全部源于已反复犯过的错误（出处见 `docs/ISSUES_AND_LESSONS.md`）。
> 把教训写进 docs 已被证明无效——我不会主动去读；**本文件是唯一的强制执行点**。

## 0. Codex review 是硬性门（PI 要求，2026-07-30）

| 时点 | 动作 |
|---|---|
| **决策前** | 提出新方向/新假设/新判据，**在写任何代码之前**交 `/codex:rescue` review |
| **实现后** | 脚本与判决逻辑写完、**在跑任何实验之前**交 Codex review |

Codex 的结论不盲信，但**它引用的每条证据必须逐条独立核实**（`grep`/`sed` 看原文）。
核实为真即接受，包括"关闭本方向"。已发生：CGT 被 Codex 判为换皮，三条证据核实全真，方向关闭（`76dcd16`）。

### 0.1 **必须 `--resume`，禁止 `--fresh`**（PI 要求 2026-07-31）

审查时**一律沿用同一 Codex 线程**：

```bash
node "$CODEX_HOME/scripts/codex-companion.mjs" task-resume-candidate --json   # 先查可恢复线程
# 然后向 codex:codex-rescue 传 --resume（不要传 --fresh）
```

**理由**：本课题的判断高度依赖上下文——十一族失败史、M1–M31、各实验的作废原因、
estimand 的定义。新线程不了解这些，会做出错误判决，或要求我重新解释已澄清的事实。

**我已犯**：RACING_MULTI、RACE-then-RUN、v3 合法性裁定三次用了 `--fresh`，
每次让 Codex 从零理解项目。若线程确实不可恢复，须在 prompt 中附上
`docs/EVIDENCE_STATE_20260731.md` 与相关 M 条目的摘要。

## 1. 提新方案 / 新信号族之前（违反过 3 次：M15 ×2、CGT）

**必须实际运行这三条命令，不得凭记忆判断"我记得没做过"：**

```bash
grep -n "停止投入\|不再" docs/RESEARCH_EXECUTION_GUARDRAILS_20260721.md   # 是否已被明令禁止
grep -rln "<机制关键词>" docs/archive/ docs/experiments/                    # 是否有失败先例
grep -rn "fixed\|learned\|对照" docs/experiments/*<同 target>*results*.md   # 同 target 是否已做过该对照
```

然后书面回答：**我的代理量 X 与反事实延迟学习效用 U 之间，缺哪一步因果箭头？**
写不出来就停。换时间尺度／换在线离线位置／换聚合方式**都不算新信号族**——
**estimand 没变就是换皮**（已犯 4 次）。

## 2. 断言某个参数/配置有影响之前（违反过：LR 日程误判）

grep 到一处用法**不等于**读懂了它。必须读完整链条：定义 → 传递 → 消费，
并用**运行时实际值**验证（checkpoint 里的 `args`、进程的实际命令行）。

反例：我 grep 到 `T_max=args.total_timesteps` 就断言 LR 日程被压缩，
实际 `actor_learning_rate == actor_learning_rate_end`，`eta_min == base_lr`，
余弦退火恒为常数。**多跑一条 `grep learning_rate_end` 就能避免。**

## 3. 启动并行任务之前（违反过：评估文件并发写）

列出每条并行链的**输出文件集合**，验证两两不相交。
`[[ -f "$OUT" ]] && skip` **只在启动瞬间检查，不是原子锁**。

```bash
pgrep -af "train_ptf" | grep -v "bash -c"   # 看清楚再动手；不要用 pgrep -f | wc -l 计数(会匹配自身)
free -g | sed -n 2p                          # 训练进程并行度硬上限 3（RAM 瓶颈，本机为共享机器）
```

**禁用 `pkill -f`**（E3，仍在犯）——用 `kill <PID>` 或 `tmux kill-session`。

## 4. 写判决 / 分析脚本时（自查发现过 2 处）

- 数据不全**必须**输出 `INCOMPLETE` 并非零退出。绝不能让缺失路径落进
  `REFUTED`/`PASS` 等实质裁决分支——"没跑完"被读成"方向被否定"是最坏的错误。
- 缺失统计要**独立扫描全部组合**，不能在前置项缺失时 `continue`（会漏报后续项）。
- 判据必须在**看到任何结果之前**提交 git，之后只允许改路径参数，不得动判据逻辑。

### 4.1 结果 commit 里一个字节的代码都不许改（违反过：P1.1，2026-08-06）

**运行结果暴露实现 bug 时，可以修——但修复不能待在结果 commit 里。**

我在 `19948c4` 干的：名义"P1.1 结果提交"，实际同时改了 `smoke_evaluator_v21.py`
与 `p0_evaluator_v2.py`（basketball 修复、S2 换 checkpoint、新增 VACUOUS 判定
**全是看到首轮结果之后加的**），然后声称"预注册 → 实现 → 纯结果三段式通过"。
**三段式的全部意义就是"判据与实现先冻结、结果不能回头改它们"，
在结果 commit 里改代码恰好取消了这个保证。** 修复内容对不对是另一回事，
一个含代码改动的 commit 不具备验证效力。

强制流程——运行结果暴露 bug 时：

```
1. 把已产出的结果如实标成 DIAGNOSTIC（文档 + 数据目录各一份声明），
   不重写 git 历史，不删原始输出，作废其 verdict 字段；
2. hotfix commit：只有代码，无结果；
3. 重新冻结（预注册若需调整则新写一份，不改旧的）；
4. 独立重跑，结果 commit 里 `git show --stat` 必须只有文档与数据文件。
```

**交付前自查**：`git show --stat <结果commit>` 逐行看，出现任何 `.py` / `.sh` 即违规。

- `verdict` 不得取 `ALL_PASS` 之类的全称词，除非**每一项**都真实执行过判定路径。
  条件式判据在前提不成立时真空为真，必须单列 `VACUOUS`，且
  **VACUOUS 的项不得进入任何科学裁决**（P1.1 的 bookshelf 即此情形）。
- smoke / 测试的判据实现必须**逐条对齐预注册原文**，不得改用"更容易通过的代理"。
  已犯：预注册要求"至少提取到一个有限 `ball_to_hoop_dist`"，
  实现写成"若有终止 episode 则不全为 INSUFFICIENT_STATE"——且 0 终止时真空通过。

## 5. 下结论之前（违反过：RACING_K 批1）

- **跨 learner 的结论必须用 learner 间方差，不是 episode 面板 SE**（M16）。
  批1 的 K\*=5000 显示 run 领先 **8.4–14.8 个 episode-SE**，看似无可争议，
  独立重复只有 1/3。正确尺度下 `t=1.57`，不显著。
- `n=3` 的 3/3 **不足以定论**，尤其当效应量接近 learner 间 SE 时——需独立重复。
- 引用跨实验数字前确认口径可比：**配对差值可比，绝对回报不可比**（我在写下这条
  几小时后就自己违反了）。
- 报告时**不得合并不同 reward 结构的 target**（M15 延伸；合并 slide/stair 曾掩盖决定性反例）。

## 6. 字段语义陷阱（已导致过结论读反）

`p0_evaluator.py` 的 `aggregate.success_count` 读自 `terminated`：
在 **hurdle 等 locomotion 任务上是"摔倒早停"计数，不是成功率**，与 return 强反向。
脚本注释里那句"成功"只对 package/truck 成立。引用前先确认当前 target 的语义。

## 7. 交付前自查

写完代码/文档后，回到本文件逐条对照，确认没有违反。
**这一步不是形式**——第 2、3、5 条的错误都是写完当时就能查出来的。

## 8. 设计新实验时（§1–§7 是执行层，这一节是设计层）

2026-07-30 的 RACING_MULTI 被外部 review 判为"不得运行"，三条致命缺陷**全是设计层的**，
而当时 §1–§7 我都过了。设计一个新的对照/判决场时，必须书面回答：

**8.1 辨别力**：写出**至少一个平凡解释**，并说明我的设计如何排除它。

> 反例（我犯的）：两个 target 用了**不同的候选集合**（`{walk,slide}` vs `{walk,stair}`），
> 我以为"正确答案不同"就是 crossover。实际上一个**全局固定排序** `slide>walk>stair`
> 就能同时通过两边。**"答案不同" ≠ "排序反转"**——
> 真正的 crossover 必须是**同一候选集合**上赢家反转。
> 另一个常见捷径："选 |U| 最大者"也能通过很多看似精巧的设计。

**8.2 混淆变量**：有没有与处理**共变**的量？

> 反例（我犯的）：sibling 臂的 behavior share 系统性高 2.4–3.3pp，且优势方向与剂量同向。
> 我识别出了它，却只是**放宽容差**——那等于把混淆写进协议。
> 正确做法是强制匹配实际剂量，或把 estimand 改写为"源+控制器"整体。

**8.3 独立重复**：是否有**新的 learner seeds**（不是同 seed 重跑）？

> 同 seed 重跑只测 CUDA 非确定性下的稳定性（E10/E15），**不是 learner 总体的抽样**。
> M24：单批 n=3 的 3/3 会被独立重复推翻（RACING_K 实测 3/3 → 1/3）。

**8.4 前提是否蕴含结论**：把 sanity/前置检查的通过条件写成命题，
验算它是否蕴含主判据。

> 反例（我犯的）：RACING_REJECT v1 的 sanity 要求"K=10000 时 9/9 全负"，
> 而这直接蕴含 `max_i U_i < 0`，使否定分支**逻辑上不可达**。

**8.5 site selection**：这个场地/候选集合是不是**知道结果之后**才选的？
是则只能声称"在这些特定案例上成立"，不得声称跨任务推广。

**8.6 是否重演了本轮刚记录的教训**：写预注册/设计前，先
`git log --oneline -15` 并读**本轮新增的 M 条目**，逐条检查当前设计是否正在重犯。

> 反例（我犯的）：`M29` 否决"可重复性 ≈ 间隔/尺度"后 **86 秒**，
> 同一推理又被写进 v3 的预注册（`dd63187` → `01dd93e`）。**写下教训 ≠ 内化教训。**

**8.7 判据切换的红线**：在**同一数据**上把已知失败的门换成已知通过的门，
即使主终点数据仍盲、即使如实披露，**也不能恢复确认性地位**
（outcome-contingent gate switching）。合法替代只有 split-sample：
旧 seeds 降级为 design data、未揭盲批作 holdout、另加新 seeds。

**执行**：以上七条写不出答案就不要动手；写完后连同设计草案一起交 Codex review（§0）。
