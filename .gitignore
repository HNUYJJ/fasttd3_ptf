# ══════════════════════════════════════════════════════════════════════
# 本仓库只收录**代码、配置与科研证据文档**。
# 一切模型权重、replay、训练产物、3D 资产一律不进版本控制。
#
# 2026-08-06 重建。原仓库因 a5cec9d 在补 .gitignore 之前就 commit 了
# 2.4GB 的 replay.pt，导致 .git 膨胀到 59GB 且超过 GitHub 100MB 单文件硬限制、
# 永久无法推送。本文件的规则顺序即为防止重蹈覆辙。
# ══════════════════════════════════════════════════════════════════════

# ── 1. 权重与张量（任何位置，无例外）─────────────────────────────────
*.pt
*.pth
*.ckpt
*.ckpt.*
*.safetensors
*.npy
*.npz
*.h5
*.pkl

# ── 2. 训练产物目录 ─────────────────────────────────────────────────
artifacts/
# 例外：anchor bundle 的 manifest / checksums 是**审计链证据**（小 JSON）。
# 大张量（learner/replay/rng）已由 §1 的 *.pt 规则排除，而 checksums 本身
# 就能校验那些不进版本控制的张量。
!artifacts/
!artifacts/**/
!artifacts/**/manifest.json
!artifacts/**/checksums.json
models/
checkpoints/
wandb/
logs/
videos/
outputs/
runs/
tmp/

# ── 3. 3D 资产（HumanoidBench 上游公开资产，见 README「补回资产」）──
*.obj
*.stl
*.STL
*.mtl
*.dae
*.skn
*.png
*.jpg
*.jpeg
*.tga
*.hdr

# ── 4. 论文 PDF（第三方版权）─────────────────────────────────────────
papers/
*.pdf

# ── 5. Python / 系统生成物 ───────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/
.DS_Store
MUJOCO_LOG.TXT

# ── 6. 日志：训练日志排除，但**裁决输出必须保留** ───────────────────
# docs/data/ 下的 .log 是实验裁决输出（含 VERDICT 行），属于科研证据，
# 原仓库的 `*.log` 规则曾把它们一并排除，此处修正。
*.log
!docs/data/**/*.log
!docs/**/*.log

# ── 7. 重复副本 ─────────────────────────────────────────────────────
reference_source_code/
.claude/worktrees/
