# Stability-deconfounded audit: stand-only RBO for maze.
# Controlled: weight=1, horizon=25, warmup=30k, exec_prob=0.5.
null_option: true
sources:
- name: stand
  manifest: checkpoints/official_sources/h1hand_stand/manifest.json
  obs_adapter:
    type: identity
    output_dim: 151
  action_adapter:
    type: passthrough
  action_mask:
    type: full
  compatibility_sigma: 1.5
  bootstrap:
    weight: 1.0
    horizon: 25
